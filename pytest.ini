[pytest]
pythonpath = .
testpaths =
    tests
markers =
    local_artifact: requires untracked native binaries, generated receipts, or machine-bound runtime evidence
    sdk_required: requires AFTER_EFFECTS_SDK_ROOT and a native build toolchain
    prebuilt_required: requires a named native build artifact produced before pytest
    built_artifact: requires workers, probes, and input fixtures built from this checkout

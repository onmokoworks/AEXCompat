# AEXCompat Compatibility Status (2026-07-16)

## Scope and status rules

This document summarizes the current host as an image-effect compatibility host. `Implemented` means that the current source publishes the relevant ABI and has focused local tests. It does not imply Adobe After Effects pixel equivalence. `Partial` identifies known semantic, slot, depth, or runtime-verification gaps. `Not implemented / unproven` is used whenever the available source, tests, or evidence do not prove compatibility.

Authoritative inputs for this snapshot are `minihost/src/l2_main.cpp`, the focused tests under `tests/`, and dated evidence under `analysis/`. Complete After Effects compatibility is not claimed.

## PF suites

| Area | Status | Current evidence | Remaining gap |
|---|---|---|---|
| PF Pixel Data Suite v1/v2 | Implemented | Current source publishes v1/v2; focused tests and native self-test cover ARGB8, ARGB16, ARGB32F, GPU-float access, format validation, and fail-closed errors. | Adobe runtime acquisition and pixel-oracle comparison remain unproven. |
| PF ANSI Suite v1 | Implemented | All 19 SDK slots are wired; focused tests cover slot order, finite fail-closed numeric behavior, and guarded string callbacks. | Exact Adobe behavior for exceptional math inputs is not oracle-verified. |
| PF Iterate8/16/Float v1/v2 | Implemented locally | All published slots are wired for both suite versions: iterate, origin, non-clipping origin, 8-bit LUT, and generic iteration where applicable. ARGB8/16/32F share a bounded typed iterator. It reports monotonic row progress over `progress_base..progress_final`, checks abort between rows, stops pixel callbacks after cancellation, and preserves pixel-callback error priority. ABI layout assertions, focused tests, a 4-row `10..14` progress matrix, second-row cancellation, and native self-test pass; all three workers build in Release. | Exact AE scheduling for `PF_Iterations_ONCE_PER_PROCESSOR`, callback cadence for unusual rectangles, and pixel-oracle equivalence remain unproven. |
| PF World Transform Suite v1 | Partial | Composite, blend, convolve, copy/copy_hq, transfer_rect, and single-matrix transform paths are wired for the supported ARGB depths; focused/native and real-PF-AEX probes cover substantial subsets. | Exact AE pixel behavior remains incomplete. `transform_world` multi-matrix behavior is not implemented from an oracle; the AE probe is blocked by unresolved PiPL registration visibility. |
| PF Sampling 8/16/Float | Partial | Nearest, subpixel, and area callbacks are source-wired. The current rebuilt probe now renders successfully with the current authenticated source and worker, reproduces the historical internal output hash, and reports zero exceptions with intact guards and balanced suite leases. | ARGB16/ARGB32F numeric runtime vectors and an Adobe pixel oracle remain pending. |
| PF Fill Matte Suite v2 and legacy callbacks | Implemented locally | Suite fill/premultiply callbacks for 8/16/float are source-wired. The legacy utility table now publishes fill, premultiply, premultiply-color, fill16, and premultiply-color16 at the frozen SDK offsets. Both workers pass the guarded native self-test and focused regression matrix. | Full Adobe pixel-oracle coverage, especially rounding and straight/premultiplied edge cases, is not established. |
| PF Path Data Suite v1 | Implemented locally | All 11 slots are source-wired. Segment preparation uses bounded adaptive De Casteljau subdivision over a control-point snapshot, arc-length evaluation uses the resulting cumulative table, and first derivatives are analytic unit tangents. Native tests cover straight, quarter-circle, S-curve, zero-length, closed-path, stale/foreign handles, double cleanup, balanced ownership, fail-closed error codes, allocation exceptions, and synchronized prep access; PathMaster and focused path regressions pass. A reversible RGB payload probe now records frequency and length-boundary observations with a versioned CRC-protected format. | The boundary probe is built but has not yet been run in AE, so exact error codes, numeric tolerances, and pixel equivalence remain oracle-unverified. |
| PF AE Channel Suite v1 | Implemented with native providers | The frozen five-slot ABI remains intact. External manifests are promoted to immutable native planes carrying type, native data type, dimensions, signed padded stride, origin, downsample, coordinate space, units, and rational sample time. Checkout preserves signed stride/padding through all nine conversions and pins the selected sample receipt through checkin. Classic and SmartFX publish pre-effect alpha as COVR only under an explicit contract; ARGB8/16/32F normalize to the same coverage values. Provider state and generation are MFR-safe, with 2,048 concurrent native-oracle checkouts. | RGBA is never used to fabricate depth, normals, motion, object, or material channels. AE-specific coordinate systems/units and duration interval aggregation beyond the observed exact/hold-at-start rule still require native AE oracle captures. |
| PF Sampling 8/16/Float Suites | Implemented for the measured depth matrix | One real probe AEX now renders through ARGB8, ARGB16, and ARGB32F workers. Probe-side and independent Python oracles validate nearest, subpixel, area, edge, outside, and every output pixel from the input pattern. All depths report one balanced suite lease, intact guards, and zero SEH exceptions. | Adobe pixel-oracle equivalence and broader quality/mode combinations remain pending. |
| PF Batch Sampling Suite v1 | Partial, fail-closed | The official 4-slot/32-byte suite is published. Begin/end use the bounded legacy sampling ledger and reject null, duplicate, foreign, cross-thread, quality, and mode mismatches. The deprecated getter slots have official getter signatures but always clear output and return unsupported because Adobe exposes the returned callable only as opaque `void*`. | Getter callable execution remains intentionally unavailable until a typed Adobe source, symbol, or isolated AE oracle establishes its ABI. |
| PF Color Suite / Color16 / ColorFloat v1 and legacy callbacks | Implemented | All 24 suite slots and the eight legacy 8-bit callbacks are wired to shared HLS/YIQ/scalar implementations. ABI offsets, finite/HDR handling, alpha preservation, focused tests, and native self-test are present. An AE oracle probe is built. | The AE oracle has not yet been captured, so host-specific integer rounding, achromatic hue, exceptional float behavior, and bit-exact AE equivalence remain unproven. |
| PF ColorParamSuite v1 | Implemented | The frozen one-slot ABI is typed and lease-managed. Current/default colors resolve through host parameter state without truncating 16-bit, float, HDR, or negative values; native and focused tests cover ARGB order and fail-closed inputs. | Adobe runtime acquisition and behavior for ambiguous quantized color definitions remain oracle-unverified. |
| PF AE App Suite v4/v5/v6 | Implemented for headless host semantics | The frozen 11/12/15-slot layouts are published at SDK versions 6/7/1. Background/theme color, language, personal information, render-engine state, custom-UI invalidation, and bounded v6 progress-dialog ownership have explicit callbacks. The one-byte `PF_Boolean` ABI is guarded, invalid contexts fail closed, and App/Supervisor/custom-UI focused regressions pass. | Font, cursor, mouse, coordinate conversion, global color sampling, and interactive color selection require a real UI context; the headless host intentionally does not fabricate those observations. Exact AE UI values remain unverified. |
| PF AE Adv Time Suite v4 | Implemented for deterministic headless display policy | The frozen five-slot ABI is published at SDK version 4. Format, display-preference, and frame-count callbacks use a declared 30 fps frame display with starting frame zero, checked rational arithmetic, bounded 32-byte strings, one-byte booleans, and fail-closed null/scale/overflow handling. A current native AEX probe called every slot, recorded zero exceptions, intact guards, balanced leases, deterministic zeroed struct padding, and exact/partial frame counts in an authenticated atomic sidecar. | This deterministic policy does not claim to reproduce user-configurable AE timecode, feet-and-frames, source-timecode, or drop-frame display. AE formatting remains oracle-unverified. |
| PF Cache On Load Suite v1 | Implemented for deterministic no-persistent-cache policy | The official v1 ABI is published as exactly one slot at offset zero. `PF_SetNoCacheOnLoad` accepts only the hosted effect identity and a canonical 0/1 boolean, rejecting foreign effects and other values without changing policy. The host has no persistent startup plug-in cache; the accepted decision is retained explicitly so every worker observes the same deterministic policy. Focused name/version, ABI, SDK-signature, identity, boolean-validation, and policy tests pass; all workers build in Release. | Suite acquisition and behavior have not been run against an After Effects oracle; AE startup-cache lifecycle equivalence remains unproven. |
| PF AE Adv Item Suite v1 | Implemented for deterministic headless item state | The frozen five-slot ABI is published in SDK order. Time-step moves use a render-local cursor in the thread-local loaded-effect context and checked forward/backward multiples of the hosted `time_step`, without mutating shared input bytes. Touch and force-rerender perform generation-safe project invalidation; rerender invalidates staged item worlds through that generation boundary. World-taking callbacks require the active render context, locate the registered render world, then re-read and revalidate its data, rowbytes, width, and height descriptor fields before use. Active/enabled safely reports disabled because no UI context is hosted. Focused ABI/semantics/probe-build tests pass, the SDK-typed five-slot probe builds in Release, and all workers build in Release. | The probe has not been run in After Effects. Exact AE UI active/enabled behavior, timeline side effects, invalidation scope, and runtime equivalence remain unverified. |
| PF Helper Suite2 v2 | Implemented for hosted UI tool state | The frozen three-slot ABI is published under `AE Plugin Helper Suite2`. Extended tools validate the full SDK enum range, use thread-safe state isolated by hosted comp/layer/effect-controls context, reject Set calls outside a proven active UI event, return `NONE` in headless context, and reset on UI/global teardown. | Clipboard parsing is intentionally fail-closed because the headless host has no authoritative AE clipboard service. Exact AE tool persistence outside hosted events remains unverified. |
| PF Param Utils Suite v3 | Implemented locally for bounded hold/linear timelines including ARBITRARY keys | All nine frozen ABI slots are typed and non-null. The broker and workers strictly validate the versioned animation sidecar. Scalar/color/component timelines and flatten-byte ARBITRARY keys are bounded by per-key, per-parameter, total-key, total-byte, and JSON limits. ARBITRARY keys are independently UNFLATTENed, adjacent key Handles are passed to ARB_INTERP with a key-interval amount, and replaced/unselected/result Handles are disposed across success, failure, and teardown. The host now preallocates the interpolation output with ARB_NEW, matching ColorGrid and AE's practical callback convention while still accepting a replaced result Handle. A current two-key ColorGrid run proves amount 0.5, three distinct endpoint/midpoint pixel hashes, zero exceptions, and balanced ownership. | Bezier/ease interpolation, multi-effect concurrency, and Adobe pixel-oracle equivalence remain pending. |
| PF Batch Sampling Suite v1 and legacy begin/area/end | Partial | The legacy utility table now publishes begin, subpixel, area, and end at the frozen offsets. Begin/end use a bounded session ledger with world validation and reject duplicate, foreign, mismatched, or cross-thread completion. A dedicated AE oracle AEX is built and records suite availability, all four slots, lifecycle results, `PF_SampPB` changes, and opaque pointer presence without invoking private ABI. | The suite itself remains unpublished until the AE oracle establishes current `get_batch_func*` behavior. Adobe intentionally exposes those return values as opaque pointers and deprecated the API, so callable batch semantics will not be guessed. |

## Render paths

| Path | Status | Current evidence | Remaining gap |
|---|---|---|---|
| Native AEX discovery and lifecycle | Implemented for tested fixtures | Loader, command dispatch, parameter setup, render lifecycle, and bounded suite acquisition are exercised by native and real-AEX fixtures. Approved L2, Classic, SmartFX, interactive image/audio, UI, sequence, options-dialog, and AEGP helper paths now use the restricted secure-launch transaction with authenticated worker staging. GLOBAL_SETUP, ABOUT, PARAMS_SETUP, and GLOBAL_SETDOWN share the worker SEH boundary, with the failing selector and compatibility error recorded in diagnostics. The desktop harness recursively resolves same-directory DLLs referenced by normal and delay-load PE import tables, bounds the graph to 64 dependencies / 1 GiB / 4,096 directory entries / 512 MiB per PE, excludes API-set and System32 names from automatic approval, hashes every dependency, and promotes the result through the existing validated session manifest before automatic parameter inspection. A repeatable four-fixture discovery gate passes Paramarama (8 standard parameters), ColorGrid (ARBITRARY), PathMaster (PATH), and SmartyPants (SmartFX-oriented setup). | Coverage is not proof that arbitrary third-party AEX binaries load successfully. Vendor runtimes outside the AEX directory and plug-ins that discover modules dynamically still need explicit dependency selection or environment-specific policy. Non-ASCII adjacent DLL names are intentionally not auto-promoted because the current manifest and Windows-loader case-folding models differ. |
| Classic image render | Implemented for tested effects | ARGB8/16/32F worlds, parameterized rendering, rowbytes-aware transport, and image output are covered by focused tests and dated real-AEX evidence. Deterministic and interactive Classic paths now create fresh sealed trees, authenticate the render worker, forbid normal-token fallback, and pass approved/session image tests. | Unsupported suites and effect-specific host assumptions can still prevent render. Session-approved adjacent dependencies require explicit manifest support. |
| Adobe SDK Supervisor lifecycle | Implemented for the recorded fixture | Sequence setup now precedes conditional UI on L2, classic render, and SmartFX paths. Old public Stream v2, Dynamic Stream v2, Effect v2/v3, and App v4 calls used by Supervisor are hosted; all three recorded worker runs exit zero. | This proves the named Supervisor fixture, not arbitrary UI-supervising effects or every legacy AEGP slot. |
| AEGP Effect Suite 4 parameter definitions | Implemented locally for the bounded synthetic scene | `AEGP_GetEffectParamUnionByIndex` is published at frozen slot 3 / x64 offset 24. It accepts index 0 through 4, returns definition metadata independently of selector-local PF buffers, leaves outputs untouched on invalid index/null/stale-effect failures, and does not claim to return current values. ABI, source-contract, and native self-tests pass. | The remaining Effect Suite 4 slots are not thereby complete. Rich popup/arbitrary definition unions and behavior inside Adobe After Effects still need fixture and oracle coverage. |
| AEGP Layer Suite source-item lookup | Implemented locally for the bounded synthetic scene | `AEGP_GetLayerSourceItem` is now published at frozen slot 4 / x64 offset 32 for hosted Layer Suite v11, v14, and v15. It accepts each registered synthetic layer, returns the composition item, and leaves caller output untouched for null/foreign handles or null output. ABI/source tests and all three native worker self-tests pass. | The current AeTimelineSync build does not call this slot, so a dedicated real AEGP fixture and Adobe runtime behavior remain pending. |
| AEGP Item Suite 9 type lookup | Implemented locally for the synthetic composition item | `AEGP_GetItemType` is published at official slot 5 / x64 offset 40 in the 208-byte Suite 9 table. The native scene self-test proves the composition type, foreign/null rejection, output preservation, and bounded successful-call telemetry. | Additional footage/folder item models and Adobe runtime equivalence remain pending. |
| AEGP Effect Suite 4 installed catalog | Implemented locally for the hosted catalog | Slots 11, 12, 13, 14, and 15 now provide installed count, `NONE -> 3001 -> NONE` enumeration, display name, match name, and synthetic category from one immutable catalog. ABI offsets 88/96/104/112/120 and the 176-byte table are SDK-compiled; null/unknown keys preserve outputs and strings are bounded/NUL-terminated. | The catalog currently contains one hosted effect. Apply/delete/duplicate and a dynamic multi-effect layer model remain pending. |
| AEGP Keyframe Suite 5 | Implemented locally for bounded stream mutations | All 22 official slots are frozen by the compiled SDK probe and implemented, including spatial tangents and per-dimension temporal ease. The native mutation self-test covers ordered insertion, values, flags, interpolation, batched add/cancel/commit, labels, tangent/ease roundtrips, dimension checks, foreign/stale/null rejection, failure output preservation, 14 mutations, eight ownership rejections, and balanced lifetimes. | A real AEGP fixture and Adobe runtime equivalence remain pending. |
| AEGP Stream Suite 6 value mutation | Implemented locally for hosted mask streams | `AEGP_SetStreamValue` is published at official slot 15 / x64 offset 120. OneD/TwoD values are range- and finiteness-checked before atomic commit. Mask outlines use independent checkout snapshots and deep-copy commit, so edits do not alias the scene before Set and remain valid after the caller disposes its value. Foreign, stale, mismatched, keyframed, malformed, and double-disposed values fail closed. | The hosted model currently covers mask opacity, expansion, feather, and outline streams; broader AE stream types and a real Projector-style AEGP fixture remain pending. |
| AEGP Layer Render Options Suite v2 | Implemented for hosted effect-frame receipts | The official 15-slot/120-byte v2 ABI is published alongside v1, including `NewFromDownstreamOfEffect`. Upstream, finalized all-effects, and finalized downstream worlds follow one bounded receipt conversion path with rational-time validation, ARGB8/16/32F conversion, downsample, matte handling, synchronous/asynchronous parity, and balanced receipt ownership. A native pixel oracle distinguishes all three effect boundaries; an unfinalized downstream request fails closed instead of recursively re-entering the active selector. | Mapping arbitrary third-party AE effect-stack identities and non-current-time frame production still needs broader scene modeling and Adobe runtime oracle comparison. |
| AEGP Render Options Suite v4 / Render Suite v5 item checkout | Implemented for hosted final item stages | The official 23-slot options ABI is connected to immutable final item/comp stages instead of production synthetic pixels or recursive effect execution. Stage keys bind item, rational time/time-step, quality, guide-layer state, depth, and project generation. Receipt transforms cover ARGB/BGRA 8/16/32F, ROI, downsample, frame/upper/lower field selection, straight/premultiplied-black/premultiplied-background output, rendered regions, concurrent receipt pinning, and generation-aware caching. A per-thread item/time/generation stack rejects direct and indirect cycles while allowing nested checkouts of distinct items. | The current scheduler publishes final stages primarily for the hosted comp item. Field selection and premultiplied-background color follow declared deterministic headless policies; broader item registries and exact AE pixel oracles remain pending. |
| PF Color Settings Suite / AEGP ColorSettingsSuite6 | Implemented for bounded headless sRGB policy | The SDK-compiled ABI is published at acquisition version 7 with 20 typed slots/160 bytes. The host provides owned sRGB and linear-sRGB profiles, bounded ICC import/export, UTF-16 descriptions, gamma/RGB queries, ARGB8/16/32F working-to-view conversion with alpha preservation and safe in-place operation, and explicit OCIO-disabled results. Generated linear-sRGB ICC data is independently parsed and accepted by Windows WCS; malformed, foreign, stale, double-disposed, oversized, and exhausted profile operations fail closed. | Arbitrary imported-profile transforms, OCIO configuration/transforms, and Adobe blending tables are not implemented. Exact AE color-management output remains oracle-unverified. |
| PF Effect Sequence Data Suite v1 | Implemented | The SDK-frozen one-slot/8-byte ABI returns the currently live unflattened sequence handle as a borrowed `PF_ConstHandle`. A bounded effect-instance registry publishes successful setup/resetup generations, excludes flattened temporaries, invalidates before setdown, and clears on failure/SEH cleanup. Classic, Render, and Smart workers each verify setup-before-use, stale/foreign/null rejection, balanced ownership, and 2,048 concurrent MFR reads of one stable handle identity. | Exact AE behavior for an effect with no sequence data, calls from flatten selectors, and resetup/setdown races remains oracle-unverified; these cases fail closed instead of being guessed. |
| SmartFX CPU render | Partial | Smart pre-render/render, ROI, deep-color and multiple-input paths have fixture and selected real-AEX evidence. Deterministic and interactive Smart paths use fresh sealed trees and authenticated restricted worker staging; approved ScatterMap outputs match, and GPU-attempt CPU fallback creates a fresh secure transaction. | Broad third-party compatibility and exact AE scheduling/checkout semantics remain unproven. |
| Multiple image inputs | Implemented for exact supplied rational times | Selected SDK and real-AEX fixtures cover multi-layer input and checkout behavior. The broker accepts up to 64 bounded secondary images using the legacy static form or a versioned `slot + rational time` form. Mathematically duplicate slot/time keys fail closed; Classic checkout and SmartFX pre-render resolve exact rational times across ARGB8/16/32F worlds, and a timed slot has no silent static fallback. A direct Classic run selected slot 6 at exactly `1/2`. A dedicated SmartFX AEX checks out the current time plus two distinct non-current rational times from one layer slot, enforces three balanced checkout/checkin pairs, and produces independently verified weighted pixel output at all three depths. | Hold/nearest sampling outside an exact supplied key is intentionally not guessed. Broader nested-comp scheduling and Adobe runtime pixel comparison remain pending. |
| World Transform transfer modes | Partial | Public transfer modes from COPY through DIVIDE, including HSL, alpha, dissolve, additive-premul, and alpha-add families, are implemented with focused/native probe coverage. | Adobe rounding, quality, mask, and per-depth pixel equivalence are not fully oracle-verified. |
| Transform multi-matrix motion blur | Not implemented / unproven | A two-matrix AE oracle probe and runner exist. | AE 2025 did not expose the probe among 1,582 effects; PiPL registration visibility is unresolved, no PNG was captured, and no weighting or pixel claim is valid. |
| GPU render/device worlds | Partial | GPU negotiation, memory, CPU fallback, and selected CUDA/OpenCL/DirectX/OpenGL fixture boundaries have dated evidence. Authenticated GPU dispatch now requires an unexpired backend-matched runtime-module policy, a classified module report, and a session-bound identity immediately before the first GPU launch; missing or mismatched authorization fails closed. CPU execution and a fresh CPU fallback transaction do not inherit GPU exceptions. | Vendor UMD/ICD inventory provisioning, broad real-device execution, and cross-device pixel equivalence remain pending. |

## Safety

| Area | Status | Current evidence | Remaining gap |
|---|---|---|---|
| Broker image admission | Implemented for current transport limits | Checked arithmetic validates dimensions, pixels, stride, and byte size; RGBA transport is capped at 64 MiB and internal images at 256 MiB; decode is limited to 4096 x 4096; raw outputs are metadata-checked before allocation. Broker-owned transport files older than 15 minutes are reclaimed only when their exact generated name, canonical direct parent, regular-file type, non-reparse status, age, size, and modification time remain valid immediately before deletion. Tests preserve new, unknown, directory, and symlink entries. | Limits are host policy rather than proof of AE equivalence; additional formats require equivalent validation. Hard process termination can still leave data visible until the next render triggers bounded cleanup. |
| Native world bounds and coordinate handling | Partial | Format/rowbytes/dimension guards, bounded snapshots, and non-finite/out-of-range projective-coordinate rejection are present in tested paths. | Capacity of externally supplied native buffers cannot always be proven from `PF_EffectWorld` metadata alone; ABI alignment and remaining suite boundaries need continued audit. |
| Failure isolation | Partial | Schema-v2 and session-approved paths copy and reauthenticate plug-in/dependency trees, authenticate/stage workers, apply protected DACLs, create restricted tokens, and start suspended inside bounded Jobs. Adversarial E2E covers read-only access, cross-stage modification denial, tamper, timeout, and cleanup. Native audit v2 accumulates post-load, selector, SEH, GPU lifecycle, and pre-unload module snapshots; the broker independently rejects missing, malformed, truncated, incomplete, inconsistent, or unknown-module reports on successful production launches. GPU policy authorization is backend- and session-bound and is revalidated at dispatch. | This is an integrity-oriented compatibility boundary, not a confidentiality or general filesystem sandbox. Production vendor-policy generation still needs Authenticode/catalog, adapter, driver-package, and OS identity capture. |

## Real AEX and AE verification

| Evidence class | Status | Notes |
|---|---|---|
| SDK and project-owned PF AEX fixtures | Implemented for the recorded cases | Selected classic, SmartFX, deep-color, multi-input, transform, transfer, convolve, parameter, path, audio, UI, and GPU-boundary cases have dated evidence. Each result proves only its named fixture and scenario. |
| Selected real AEX renders | Partial | ScatterMap and other recorded real-AEX matrices demonstrate native loading, parameter handling, image input, and image output in covered cases. They do not establish universal AEX compatibility. |
| Adobe After Effects pixel oracle | Partial / mostly pending | Multi-matrix, Path Data boundary, and Color Suite probes are built. A hash-pinned three-probe bundle and non-self-elevating install/remove manager now provide one reversible AE installation operation while refusing to touch a running AE session or an unverified payload. No AE values from this bundle have been captured yet, so broad operation-by-operation pixel equivalence is not established. |
| Runtime evidence freshness | Current for the regression suite | AEGP receipt/cancel/layer-receipt and Composite16 probes were rerun against the current workers, and their source, worker, probe, report, and output hashes were refreshed. The focused evidence checks pass. |

## Current priority order

1. Install the verified three-probe oracle bundle with one explicit administrator operation, capture multi-matrix, Color, and Path Data observations, then remove the verified bundle.
2. Implement the observed multi-matrix weighting semantics without guessing undocumented behavior.
3. Reconcile Color rounding/exceptional-value and Path Data boundary behavior against the captured AE records.
4. Add AE-oracle-backed normals/motion-vector coordinate and unit contracts, then exercise auxiliary channels with real effects.
5. Capture PF Batch Sampling availability and opaque batch-function behavior in AE, then implement observable lifecycle behavior without guessing private callable ABI.
6. Continue real-AEX and AE pixel-oracle matrices across depth, quality, masks, transfer modes, SmartFX, and GPU boundaries.

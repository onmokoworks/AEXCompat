name: AE SDK tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ae-sdk-tests-${{ github.ref }}
  cancel-in-progress: true

# cargo test と source-only (SDK なし) の pytest は windows-clean-clone.yml が
# 担当する。この workflow は SDK 込み検証 (--run-sdk-tests) 専用。
jobs:
  sdk-backed:
    name: pytest with AE SDK (windows)
    runs-on: windows-latest
    # fork (untrusted) PR では実行しない: SDK asset は collaborator 限定であり、
    # PR 側コードに asset へ届く token 付きで pytest を走らせない (fail-closed)。
    # 現状 private repo で same-repo branch PR のみなので挙動は変わらない。
    # A public repository/fork must never fetch the private SDK release asset.
    # Public PRs still receive the source-only windows-clean-clone workflow.
    if: github.event.repository.private && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    timeout-minutes: 120
    env:
      SDK_RELEASE_TAG: ci-sdk-ae25.2
      SDK_ASSET: AfterEffectsSDK-ae25.2-win.zip
      SDK_SHA256: 23F724D6B9C3E8F66B6C1A449EDA1F4C0B73D5A1C2706734C258EBF00E461F21
    steps:
      - uses: actions/checkout@v4

      # Python 版は .python-version (3.12) に従う。3.14 は OpenEXR の
      # win_amd64 wheel が無く、毎回ソースビルドで約 2.5 分かかるため上げない。
      # setup-uv は v8 から floating major tag (@v8) を出していないので full tag で pin する。
      - uses: astral-sh/setup-uv@v8.3.2
        with:
          enable-cache: true

      # Gyroflow 方式: SDK zip を取得・展開して AFTER_EFFECTS_SDK_ROOT を通す。
      # asset は private release (collaborator 限定) にあり、hash mismatch は fail-closed。
      - name: Fetch After Effects SDK
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $zip = Join-Path $env:RUNNER_TEMP $env:SDK_ASSET
          gh release download $env:SDK_RELEASE_TAG --repo $env:GITHUB_REPOSITORY --pattern $env:SDK_ASSET --output $zip
          $hash = (Get-FileHash -Algorithm SHA256 $zip).Hash
          if ($hash -ne $env:SDK_SHA256) { throw "SDK asset hash mismatch: expected $env:SDK_SHA256, got $hash" }
          $dest = Join-Path $env:RUNNER_TEMP 'aesdk'
          Expand-Archive -Path $zip -DestinationPath $dest -Force
          $root = Join-Path $dest 'AfterEffectsSDK'
          if (-not (Test-Path (Join-Path $root 'Examples\Headers\AE_Effect.h'))) {
            throw 'extracted SDK is missing Examples\Headers\AE_Effect.h'
          }
          "AFTER_EFFECTS_SDK_ROOT=$root" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Install Python dependencies
        run: uv sync --locked

      # built_artifact テスト (test_render_video_batch_cli) が
      # broker/target/release/broker.exe を実行する。ビルドしないと
      # "broker.exe is not built" で skip し、後段の skip gate が job を落とす
      # (#349)。
      - name: Build the broker CLI (release)
        working-directory: broker
        run: cargo build --release --locked -p aexcompat-broker --bin broker

      # built_artifact テスト (#73) 用: minihost workers を Ninja でビルドする。
      # docs/BUILD_REQUIREMENTS.md の指示どおり target\minihost-build 直下に
      # フラット配置する (single-config)。
      - name: Build minihost workers (Ninja)
        shell: cmd
        run: |
          for /f "usebackq delims=" %%i in (`"%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set "VSROOT=%%i"
          if not defined VSROOT exit /b 1
          call "%VSROOT%\VC\Auxiliary\Build\vcvars64.bat" || exit /b 1
          cmake -S minihost -B target\minihost-build -G Ninja -DCMAKE_BUILD_TYPE=Release || exit /b 1
          cmake --build target\minihost-build || exit /b 1
          dir /b target\minihost-build\aex_*.exe

      # ヘッダ依存が実際に記録されているかを確認する (#657)。cl の /showIncludes は
      # ローカライズされるため、configure 時と build 時で言語が食い違うと ninja は
      # 依存を 0 件記録し、以後ヘッダを変えても再ビルドされない。それに気づかず
      # 古いヘッダでコンパイルされたオブジェクトが混ざった worker が全 AEX で
      # access violation を起こした (#651)。runner は毎回クリーンなので普段は
      # 通るが、ビルド設定の変更でこの追跡が壊れたらここで落ちる。
      - name: Verify minihost header dependency tracking
        shell: pwsh
        run: pwsh -File tools\verify-minihost-build-deps.ps1

      # trace writer の動的テスト (test_production_worker_trace.py /
      # test_instruments_trace_writer.py の handle ベース検証) を CI で実走させる
      # ため instruments の trace_writer_selftest をビルドする (#250)。この target は
      # trace_writer.cpp + windows.h のみで SDK 不要なので AE_SDK_ROOT は渡さず、
      # pf-null-echo はスキップさせる。出力先はテストが参照する target\instruments-build。
      - name: Build trace writer selftest (Ninja)
        shell: cmd
        run: |
          for /f "usebackq delims=" %%i in (`"%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set "VSROOT=%%i"
          if not defined VSROOT exit /b 1
          call "%VSROOT%\VC\Auxiliary\Build\vcvars64.bat" || exit /b 1
          cmake -S instruments -B target\instruments-build -G Ninja -DCMAKE_BUILD_TYPE=Release || exit /b 1
          cmake --build target\instruments-build --target trace_writer_selftest || exit /b 1
          dir /b target\instruments-build\trace_writer_selftest.exe

      # 一部の built_artifact テストは worker を multi-config 時代の固定パス
      # (minihost-build-v18\[Release]、minihost-timed-layers\Release、
      # minihost-build\Release) から探すため、同一バイナリを複製して満たす。
      # これらのテストは frozen hash を照合しないので複製で問題ない。
      - name: Mirror workers into expected build layouts
        shell: cmd
        run: |
          for %%d in ("target\minihost-build-v18" "target\minihost-build-v18\Release" "target\minihost-build\Release" "target\minihost-timed-layers\Release") do (
            mkdir %%d 2>nul
            copy /y target\minihost-build\aex_*.exe %%d\ >nul || exit /b 1
          )

      # probe テストの入力 fixture (37x23 raw RGBA)。内容は決定論的に生成され、
      # built_artifact テストは期待値を実行時に自己計算するため内容非依存。
      - name: Generate probe input fixture
        shell: cmd
        run: |
          mkdir target\gpu-effects 2>nul
          uv run python tools\generate-oracle-rgba-input.py --width 37 --height 23 --alpha-mode opaque --out target\gpu-effects\opencl-input.png || exit /b 1
          uv run python tools\png-to-rgba-raw.py --png target\gpu-effects\opencl-input.png --out target\gpu-effects\opencl-input.rgba || exit /b 1

      # built_artifact の probe 実行テストが参照する probe .aex を事前ビルドする。
      # (一部は同一ファイル内の sdk_required テストでもビルドされるが、事前に
      # 揃えることでテスト順序への依存をなくす。)
      - name: Build probe AEX set
        shell: cmd
        run: |
          set "PSModulePath=%ProgramFiles%\WindowsPowerShell\Modules;%SystemRoot%\System32\WindowsPowerShell\v1.0\Modules;%PSModulePath%"
          for %%s in (
            build-pf-sampling-probe
            build-pf-aegp-async-layer-receipt-probe
            build-pf-aegp-external-cache-roundtrip-probe
            build-pf-aegp-fast-blur-probe
            build-pf-aegp-layer-options-probe
            build-pf-aegp-layer-receipt-probe
            build-pf-aegp-owned-world-probe
            build-pf-aegp-platform-world-probe
            build-pf-aegp-render-options4-tail-probe
            build-pf-aegp-render-suite5-probe
            build-pf-convolve-depth-probe
            build-pf-parameter-echo-probe
            build-pf-layer-param-probe
            build-pf-param-utils-animation-probe
            build-pf-smart-geometry-probe
            build-pf-smart-param-time-probe
            build-pf-transfer-mask-probe
            build-pf-transfer-rect-probe
            build-pf-transform-affine-probe
            build-pf-custom-ui-probe
            build-pf-frame-resize-probe
            build-pf-visual-audio-probe
          ) do (
            powershell -NoProfile -ExecutionPolicy Bypass -File tools\%%s.ps1 || exit /b 1
          )

      # #356: the parameter-animation integration tests locate their native
      # worker and AEX fixtures under these exact paths.  Run the target here,
      # after the clean worker/probe builds, so a missing prerequisite cannot be
      # mistaken for coverage (the tests otherwise return early when a local
      # artifact is absent).
      - name: Run native parameter-animation Rust coverage
        working-directory: broker
        shell: pwsh
        env:
          AEXCOMPAT_ALLOW_RESTRICTED_TOKEN_SKIP: "1"
        run: |
          $ErrorActionPreference = 'Stop'
          $root = $env:GITHUB_WORKSPACE
          $required = @(
            (Join-Path $root 'target\minihost-build\aex_render_worker.exe'),
            (Join-Path $root 'target\pf-layer-param-probe-build\Release\pf_layer_param_probe.aex'),
            (Join-Path $root 'target\pf-param-utils-animation-probe-build\Release\pf_param_utils_animation_probe.aex')
          )
          foreach ($path in $required) {
            if (-not (Test-Path -LiteralPath $path -PathType Leaf)) {
              throw "#356 native coverage prerequisite is missing: $path"
            }
            $file = Get-Item -LiteralPath $path
            $hash = (Get-FileHash -LiteralPath $path -Algorithm SHA256).Hash.ToLowerInvariant()
            Write-Host "coverage_artifact=$($file.FullName) size=$($file.Length) sha256=$hash"
          }
          $commit = git -C $root rev-parse HEAD
          Write-Host "coverage_source_commit=$commit"
          cargo test -p aexcompat-broker --test parameter_animation --locked -- --show-output 2>&1 |
            Tee-Object -FilePath (Join-Path $root 'parameter-animation-output.txt')
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # A hosted runner may be unable to create the sealed worker
      # (#335).  Keep that limitation visible while retaining the exact target
      # and prerequisite checks above.
      - name: Report parameter-animation sealed-worker skips
        shell: pwsh
        if: always()
        run: |
          $log = Join-Path $env:GITHUB_WORKSPACE 'parameter-animation-output.txt'
          if (-not (Test-Path -LiteralPath $log)) {
            Write-Host 'parameter-animation produced no log; the coverage step did not start.'
            exit 0
          }
          $skips = Select-String -Path $log -Pattern 'cannot launch a sealed worker'
          if ($skips) {
            Write-Host "::warning::$($skips.Count) parameter-animation tests did not run: this runner cannot launch a sealed worker (issue #335)"
            $skips | ForEach-Object { $_.Line }
          } else {
            Write-Host 'Every parameter-animation integration test ran without a sealed-worker skip.'
          }

      # built_artifact テスト (test_sdk_backwards_audio_worker) が参照する
      # SDK example fixture を事前ビルドする。pytest の収集順は
      # test_sdk_backwards_audio_worker.py (audio) を fixture_build.py より先に
      # 並べるため、fixture ビルドを pytest 前に済ませてテスト順序依存をなくす。
      - name: Build SDK example fixtures (built-artifact deps)
        shell: cmd
        run: |
          set "PSModulePath=%ProgramFiles%\WindowsPowerShell\Modules;%SystemRoot%\System32\WindowsPowerShell\v1.0\Modules;%PSModulePath%"
          powershell -NoProfile -ExecutionPolicy Bypass -File tools\build-sdk-backwards.ps1 || exit /b 1

      # cmd で実行する: pwsh の step から spawn すると、テストが起動する
      # powershell.exe (5.1) が pwsh 用の PSModulePath を継承してしまい、
      # Get-FileHash 等の組み込み cmdlet を解決できなくなる。
      - name: Run pytest with SDK and built-artifact tests
        shell: cmd
        # See windows-clean-clone.yml: the skip is opt-in so a token regression
        # cannot hide as an environment limitation off CI (issue #335).
        env:
          AEXCOMPAT_ALLOW_RESTRICTED_TOKEN_SKIP: "1"
          # 前段の Build minihost workers が同一 checkout からビルド済みの
          # worker を canonical_release_worker fixture に渡し、fixture 内での
          # 二重ビルドを避ける (#681)。
          AEXCOMPAT_CANONICAL_WORKER: ${{ github.workspace }}\target\minihost-build\aex_render_worker.exe
        run: |
          set "PSModulePath=%ProgramFiles%\WindowsPowerShell\Modules;%SystemRoot%\System32\WindowsPowerShell\v1.0\Modules;%PSModulePath%"
          uv run python -m pytest -q -rs --run-sdk-tests --run-built-artifact-tests --validate-local-artifact-manifest > pytest-output.txt 2>&1
          set PYTEST_EXIT=%ERRORLEVEL%
          type pytest-output.txt
          exit /b %PYTEST_EXIT%

      # AFTER_EFFECTS_SDK_ROOT が通っていれば SDK 由来の skip は 1 件も出ないはず。
      # 同様に、worker/probe をビルド済みなので built_artifact テストが成果物
      # 不在の inline skip ("... is not built" 等) で黙って skip されたら fail。
      # The launch limitation reaches the Python side too: the video
      # batch CLI drives a live worker. That skip is legitimate on a hosted
      # runner, so the gate below must not fail on it, but it must not be
      # invisible either -- report it the way the Rust side does (#335).
      - name: Report sealed-worker skips
        shell: pwsh
        if: always()
        run: |
          if (-not (Test-Path pytest-output.txt)) {
            Write-Host 'pytest produced no log; nothing to report.'
            exit 0
          }
          $skips = Select-String -Path pytest-output.txt `
            -Pattern 'cannot launch a sealed worker'
          if ($skips) {
            Write-Host "::warning::$($skips.Count) Python tests did not run: this runner cannot launch a sealed worker (issue #335)"
            $skips | ForEach-Object { $_.Line }
          } else {
            Write-Host 'Every worker-launching Python test ran: no sealed-worker skips.'
          }

      - name: Fail if SDK or built-artifact tests were skipped
        shell: pwsh
        run: |
          $leaks = Select-String -Path pytest-output.txt -Pattern 'SKIPPED' |
            Where-Object { $_.Line -match 'set AFTER_EFFECTS_SDK_ROOT|is not built|are not present' }
          if ($leaks) {
            $leaks | ForEach-Object { $_.Line }
            throw 'SDK or built-artifact tests were skipped even though their prerequisites are provisioned'
          }

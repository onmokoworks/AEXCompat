[package]
name = "aex-clspv"
version = "0.1.0"
edition.workspace = true
license = "GPL-2.0-only"
publish = false

[dependencies]
naga = { version = "=24.0.0", default-features = false, features = ["msl-out", "spv-in"] }
sha2 = "0.10.9"
tempfile = "3.20"

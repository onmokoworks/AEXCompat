[package]
name = "aex-wgpu-compute"
version = "0.1.0"
edition.workspace = true
license = "GPL-2.0-only"

[dependencies]
thiserror.workspace = true

[target.'cfg(target_os = "macos")'.dependencies]
pollster = "0.4"
wgpu = { version = "24.0.5", default-features = false, features = ["metal", "wgsl"] }

[package]
name = "aex-wgpu-compute"
version = "0.1.0"
edition.workspace = true
license = "MPL-2.0"

[dependencies]
naga = { version = "24.0.0", features = ["wgsl-in"] }
thiserror.workspace = true

[target.'cfg(target_os = "macos")'.dependencies]
pollster = "0.4"
wgpu = { version = "24.0.5", default-features = false, features = ["metal", "naga-ir", "wgsl"] }
